{
  "name": "ingestion",
  "version": "1.0.0",
  "description": "PulseTrack Event Ingestion Service — receives events from tracked websites, validates API keys, rate limits, and pushes to Redis Streams",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "ioredis": "^5.6.1",
    "pg": "^8.20.0"
  }
}
